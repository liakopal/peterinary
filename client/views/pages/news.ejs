<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/header') %>
    <title>News and Veterinary Reports</title>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-4">
        <h1>Latest News on Dogs</h1>
        <!-- News articles display -->
        <% articles.forEach(article => { %>
            <!-- Display each article -->
        <% }) %>

        <h2 class="mt-5">Veterinary Adverse Event Reports</h2>
        <!-- Veterinary reports display -->
        <% if (veterinaryReports && veterinaryReports.length > 0) { %>
            <% veterinaryReports.forEach(report => { %>
                <!-- Display each veterinary report -->
            <% }) %>
        <% } else { %>
            <p>No adverse event reports found.</p>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
